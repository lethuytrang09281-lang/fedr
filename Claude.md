Это полное техническое описание проекта **Fedresurs Pro (Investment Intelligence Platform)**.
Документ консолидирует все архитектурные решения, стек технологий и бизнес-логику, которую мы утвердили в ходе рефакторинга. Это ваша "дорожная карта" и технический паспорт системы уровня Enterprise.
---
# 🏗 Проект: Fedresurs Pro (Investment Intelligence Platform)
**Статус:** Active Development (Release 2.0)
**Тип:** Data Lake & Decision Support System
**Цель:** Опережающий поиск, глубокая аналитика и оценка рисков инвестиционных лотов (земля под МКД/Коммерцию) с торгов по банкротству.
---
## 1. Архитектурная философия
Мы отказались от линейного скрипта-парсера в пользу **событийно-ориентированной архитектуры (Event-Driven Pipeline)** с разделением на слои.
1. **Data Lake (Холодное хранение):** Мы сохраняем "сырой" ответ источника (XML) в JSONB. Это позволяет переобучать алгоритмы и менять логику парсинга задним числом без повторных запросов к API.
2. **Strict Validation:** Данные не попадают в систему, если не проходят валидацию Pydantic v2 (защита от "битых" данных).
3. **Hybrid Intelligence:** Комбинация жестких правил (YARA/Regex) для скорости и AI (Gemini/Vectors) для понимания контекста.
4. **Resilience (Устойчивость):** Система проектируется с учетом нестабильности внешних API (Retries, Rate Limiting, Fingerprinting).
---
## 2. Технологический стек (Pro)
### Core & Network
* **Python 3.12+**: Основной язык.
* **httpx + tenacity**: Асинхронный клиент с экспоненциальной задержкой (Exponential Backoff) для защиты от банов.
* **curl_cffi**: Имитация TLS-отпечатков браузера (JA3) для обхода защит (Cloudflare/DDoS-Guard) на внешних сайтах.
### Data & Storage
* **PostgreSQL 16**: Основная БД.
* **pgvector**: Векторный поиск (Embeddings) для поиска "по смыслу" (Semantic Search).
* **SQLAlchemy (Async)**: ORM для работы с данными.
* **Alembic**: Управление миграциями схемы БД.
* **JSONB**: Хранение неструктурированных данных (Data Lake).
### Parsing & Cleaning
* **lxml**: Высокопроизводительный парсинг XML.
* **BeautifulSoup4**: Очистка "грязного" HTML внутри описаний лотов (уроки Р. Митчелла).
* **Pydantic v2**: Валидация типов данных, ИНН, очистка строк.
### Intelligence & OSINT
* **Semantic Filter (Custom)**: YARA-подобный движок правил (Regex) для классификации.
* **Google Gemini 1.5 Pro**: Финальный скоринг и резюме (Summary).
* **Checko.ru API**: Юридическая проверка контрагентов (с кэшированием).
* **Google Dorks**: Генерация поисковых запросов для поиска скрытых документов.
### Infrastructure & Observability
* **Docker & Docker Compose**: Контейнеризация.
* **Loguru**: Структурированное логирование (JSON).
* **Sentry**: Мониторинг ошибок в реальном времени.
---
## 3. Модульная структура (Pipeline)
Данные проходят через конвейер из 6 этапов:
### Этап 1: Smart Ingestion (Умный сбор)
* **Модуль:** `src/api/client.py`
* **Логика:** Асинхронный опрос API Федресурса. Использование семафоров (`asyncio.Semaphore`) для соблюдения лимита 8 req/sec. Автоматический повтор запросов при 50x ошибках.
### Этап 2: Validation & Normalization (Валидация)
* **Модуль:** `src/parser/schemas.py`, `src/parser/normalizer.py`
* **Логика:**
* XML конвертируется в Pydantic-модели.
* `BeautifulSoup` вычищает HTML-теги, скрипты и стили из описания.
* Регулярные выражения извлекают кадастровые номера и нормализуют площадь (га -> кв.м).
* Проверка ИНН на корректность (контрольная сумма/длина).
### Этап 3: Classification & Scoring (Первичный мозг)
* **Модуль:** `src/parser/classifier.py`
* **Логика:** Семантический фильтр на базе правил.
* *High Priority:* "МКД", "Ж-Зона", "Высотная застройка".
* *Trash Filter:* Отсеивание "СНТ", "Огородничество", "ЛПХ".
* Присвоение тегов и первичного балла (`yara_score`).
### Этап 4: Enrichment & OSINT (Обогащение)
* **Модуль:** `src/services/enricher.py`, `src/api/checko.py`
* **Логика:**
* **Manager Karma:** Подсчет статистики по Арбитражному управляющему (всего лотов / успешно продано).
* **Checko Integration:** Если лот интересен, идем в API Checko за данными об организаторе (банкрот/ликвидация/рейтинг).
* **Caching Strategy:** Результаты платных проверок кэшируются в БД на 30 дней.
### Этап 5: Analytics & Anomaly Detection (Аналитика)
* **Модуль:** `src/analytics/anomaly.py`, `src/analytics/predictor.py`
* **Логика:**
* **Anti-Corruption:** Поиск признаков "договорняка" (смешанная кириллица/латиница, короткий срок заявок, "бумажный документооборот").
* **Price Prediction:** Анализ графика публичного предложения. Определение оптимальной точки входа (дисконт 40-50%).
### Этап 6: Storage & Notification (Финал)
* **Модуль:** `src/database/models.py`, `src/services/notifier.py`
* **Логика:**
* Сохранение в БД (Hot Data + Cold JSONB + Vector Embedding).
* Отправка в Telegram с **Rich Media**:
* Кнопка поиска на ПКК (Росреестр).
* Сгенерированная ссылка Google Dorks (`site:fedresurs.ru "номер" filetype:pdf`) для поиска скрытых документов.
* Теги: "🏢 МКД", "🚨 Аномалия", "📉 Прогноз -40%".
---
## 4. Схема Базы Данных (Key Highlights)
* **`lots` table:**
* `raw_data` (JSONB): Полный слепок данных.
* `embedding` (Vector 1536): Для семантического поиска.
* `cadastral_numbers` (ARRAY + GIN Index): Мгновенный поиск по гео-пересечениям.
* **`managers` table:**
* `trust_score`: Рейтинг доверия к АУ.
* `checko_raw`: Кэш данных из Checko.ru.
* **`auctions` table:** Связующее звено сообщений.
---
## 5. Бизнес-ценность (Почему это круто)
1. **Скорость:** Мы получаем данные через 15-30 минут после публикации (быстрее агрегаторов).
2. **Глубина:** Мы видим то, что скрыто (через Google Dorks и анализ аномалий в тексте).
3. **Экономия:** Мы не тратим время на "мусор" (СНТ, Дачи) благодаря жесткому семантическому фильтру.
4. **Безопасность:** Мы заранее видим риски по организатору торгов (Manager Karma) и юрлицу (Checko).
5. **Масштабируемость:** Архитектура готова к внедрению AI-анализа (Gemini) и векторного поиска без переписывания ядра.
---
### Следующие шаги (Roadmap Release 3.0)
* [ ] Подключение **AI (Gemini)** для генерации текстовых саммари.
* [ ] Реализация **API сервера (FastAPI)** для веб-интерфейса.
* [ ] Внедрение **OCR-воркера** для распознавания сканов документов.