# FEDRESURS PRO — СОСТОЯНИЕ

## Дата последнего обновления: 2026-02-26

## ✅ ЧТО РАБОТАЕТ (не трогать)
- [x] Docker: app + db — контейнеры поднимаются
- [x] PostgreSQL: cadastral_index 584k, market_benchmarks 60
- [x] FastAPI на :8000 — отвечает
- [x] FedresursSearch: search_ur → get_org_messages → get_message
- [x] DealScorer: deal_score = investment - fraud*0.6 (logic/scorer.py)
- [x] Telegram: уведомления при deal_score ≥ 80
- [x] Hunter Engine: 4 стратегии написаны (early_bird, hidden_gem, public_offer, conflict_analyzer)
- [x] Antifraud Engine v2: 4 модуля написаны (benchmark, velocity, nlp, manager_karma)
- [x] document_extractor.py: парсинг PDF/DOCX (текстовый слой)
- [x] checko_client.py, rosreestr_client.py — работают
- [x] Git: один master, все изменения запушены

## ❌ ЧТО СЛОМАНО / НЕ ИНТЕГРИРОВАНО
- [ ] Hunter Engine НЕ импортирован в orchestrator.py
- [ ] Antifraud Engine v2 НЕ импортирован в orchestrator.py
- [ ] Enrichment Worker не запускается (класс есть в enricher.py, вызова нет)
- [ ] BUG-001: дублирование модели Document (document_model.py + models.py)
- [ ] BUG-002: два разных Base (base.py и models.py)
- [ ] BUG-003: перепутаны аргументы calculate_current_price в orchestrator.py ~172
- [ ] BUG-004: research.py вызывает несуществующий метод enrich_cadastral_data
- [ ] BUG-005: research.py → _get_lot_data использует несуществующие поля модели Lot
- [ ] BUG-006: research_routes.py — два сломанных импорта
- [ ] BUG-007: глобальный экземпляр orchestrator = Orchestrator() при импорте

## 🔴 ОТСУТСТВУЕТ В БД
- watchlist — early_bird.py
- managers — manager_karma.py
- manager_conflicts — conflict_analyzer.py
- price_alerts — public_offer.py

## 🔄 ТЕКУЩАЯ ЗАДАЧА
TASK-018: порядок в git и skills — завершается

## ➡️ СЛЕДУЮЩАЯ ЗАДАЧА
TASK-BUG-001: удалить document_model.py

## 📝 ЛОГ РЕШЁННЫХ ПРОБЛЕМ
- 2026-02-25: TASK-014 — фильтрация по дате в пайплайне
- 2026-02-26: TASK-018 — git порядок: master синхронизирован, feature ветки удалены
- 2026-02-26: scp hunter/ и antifraud/ скопированы на VPS

---

# CLAUDE.md — Fedresurs Pro
> Читается Claude Code автоматически. Обновлять в конце каждой сессии — добавлять новую сессию в конец файла.

---

## 🎯 ПРОЕКТ

**Fedresurs Pro** — автоматический поиск недооценённой недвижимости на банкротных торгах Москвы.
**Цель:** Здания внутри Садового кольца, до 300М рублей, максимальный дисконт.
**Долгосрочная цель:** SaaS по типу parser-api.com — API к публичным реестрам РФ.

---

## 🖥️ ИНФРАСТРУКТУРА

```
VPS (основной):  root@157.22.231.149  /root/fedr/
GitHub:          бэкап кода (data/ в .gitignore — не коммитится!)
Локальная:       quser@server — устаревший бэкап, не использовать

fedr-app-1  → FastAPI :8000
fedr-db-1   → PostgreSQL (fedresurs_db, user: postgres)
Nginx       → Vue 3 фронтенд

http://157.22.231.149         → фронтенд
http://157.22.231.149:8000    → FastAPI
http://157.22.231.149:8000/docs → Swagger
SSH: только по ключу, VPN: Xray через Германию
```

### ⚠️ Датасеты (data/ в .gitignore — не коммитятся):
```
На VPS /root/fedr/data/ — нужно проверить и при необходимости залить:

ls /root/fedr/data/

# Если нет — перенести с локальной машины:
rsync -avz ~/SOFT/FEDR/fedr/data/ root@157.22.231.149:/root/fedr/data/

# Загрузить в БД:
docker exec fedr-db-1 psql -U postgres -d fedresurs_db \
  -f /root/fedr/import_datasets.sql

Содержимое: data/cadastral/ (113 MB, 584к записей), data/sberbank/ (45 MB)
```

---

## 📁 СТРУКТУРА (проверено 18.02.2026)

```
/root/fedr/
├── src/
│   ├── main.py                    # FastAPI точка входа
│   ├── main_fix.py                # фикс (неизвестно что)
│   ├── config.py                  # настройки
│   ├── schemas.py                 # Pydantic схемы
│   ├── orchestrator.py            # ✅ ИСПРАВЛЕН 18.02 (FedresursSearch)
│   ├── api/
│   │   ├── hunter_routes.py
│   │   ├── research_routes.py     # /api/research/...
│   │   └── mock_data.py
│   ├── client/
│   │   ├── api.py
│   │   └── exceptions.py
│   ├── database/
│   │   ├── base.py
│   │   ├── models.py
│   │   └── document_model.py
│   ├── logic/
│   │   └── price_calculator.py
│   ├── services/
│   │   ├── fedresurs_search.py    # ✅ ГЛАВНЫЙ ПОИСК (переписан 17.02)
│   │   ├── external_api.py        # Parser API (КАД.Арбитр, Реестр залогов)
│   │   ├── parser_api_client.py   # базовый клиент
│   │   ├── enricher.py            # Росреестр PKK
│   │   ├── rosreestr_client.py    # ✅ создан 18.02
│   │   ├── checko_client.py       # ✅ создан 18.02, все endpoints
│   │   ├── moscow_api_client.py   # ✅ создан 18.02
│   │   ├── ingestor.py
│   │   ├── research.py
│   │   ├── xml_parser.py
│   │   └── document_extractor.py
│   ├── bot/
│   │   └── notifier.py            # Telegram @dev2dev1_bot
│   └── utils/
│       ├── config_loader.py       # читает search_config.yaml
│       └── resource_monitor.py    # CPU/RAM защита
├── fedresurs-panel/               # Vue 3 фронтенд
├── alembic/                       # миграции БД
├── search_config.yaml             # ← НАСТРОЙКИ ПОИСКА МЕНЯТЬ ЗДЕСЬ
├── docker-compose.yml
├── requirements.txt
└── .env                           # API ключи
```

---

## 🔑 API КЛЮЧИ И ЛИМИТЫ

### Parser API (parser-api.com):
```
Ключ: ede50185e3ccc8589a5c6c6efebc14cc

Проверка: curl -s "https://parser-api.com/stat/?key=ede50185e3ccc8589a5c6c6efebc14cc"

fedresurs:  250/день, 7500/месяц  (до 2026-03-15) — обновляется ежедневно
arbitr:     200/день, 200/месяц   (до 2026-03-04) — обновляется ежемесячно
reestr:     200/день, 200/месяц   (до 2026-03-04)
mosgorsud:  200/день, 200/месяц   (до 2026-03-04)
```
⚠️ arbitr/reestr/mosgorsud — использовать ТОЛЬКО для score >= 70!

### Безлимитные:
```
rosreestr_client.py   → pkk.rosreestr.ru
checko_client.py      → checko.ru          (CHECKO_API_KEY в .env)
moscow_api_client.py  → apidata.mos.ru     (a32c7b59-183e-4643-ba40-6259eeb9c8b7)
```

### Telegram: `@dev2dev1_bot` (токен в .env)

---

## 🔍 ЛОГИКА ПОИСКА

```
Регион: Москва (orgRegionID=77)
Цена:   1М — 300М ₽
Тип:    здания, МКД, офисы (фильтр по ключевым словам в коде)
```

### Pipeline (3 шага):
```
1. search_ur?orgRegionID=77       → список организаций-банкротов Москвы
2. get_org_messages?id={org_id}   → сообщения, ищем тип "торги"
3. get_message?id={msg_id}        → lots[].description + lots[].start_price
                                    ФИЛЬТР: "здание" в описании + цена до 300М
```

❌ get_trades, get_trade_messages — НЕ СУЩЕСТВУЮТ! Старые ошибки.

### Экономия запросов:
```python
asyncio.Lock()    # один запрос за раз — ОБЯЗАТЕЛЬНО
asyncio.sleep(2)  # пауза между запросами
RequestCounter    # счётчик → /app/data/api_usage.json
```

---

## 🔄 PIPELINE ОБОГАЩЕНИЯ ЛОТА

```
1. fedresurs_search.py  → адрес, цена, ИНН должника
2. rosreestr_client.py  → кадастр, площадь, координаты     (безлимит)
3. checko_client.py     → директор, статус, риск            (безлимит)
4. moscow_api_client.py → охранные зоны, ПЗЗ               (безлимит)
5. external_api.py      → reestr/arbitr ТОЛЬКО score >= 70  (лимит!)
```

---

## 📊 СКОРИНГ

```python
deal_score = investment_score - (fraud_score * 0.6)

Geography:  0-40  (Садовое = 40, ТТК = 25, МКАД = 10)
Discount:   0-40  (дисконт от кадастровой стоимости)
Liquidity:  0-20  (здание = 20, МКД = 18)
Timing:     0-10  (стадия инвентаризации)

Fraud:  связи +30 | залог >90% +20 | банкротство <60 дней +25

>= 80 → 🔥 HOT DEAL → Telegram
>= 60 → ✅ GOOD DEAL
>= 40 → ⚠️ REVIEW
<  40 → ❌ SKIP
```

---

## 🗄️ БАЗА ДАННЫХ

```
DB:    fedresurs_db  ← (НЕ fedr!)
User:  postgres      ← (НЕ fedruser!)
```

### Таблицы (проверено 18.02.2026 вечер):
```
✅ alembic_version  — версия: a881e873d6f2
✅ cadastral_index  — 584,354 записи (кадастр Москвы) 🎉
✅ market_benchmarks — 60 записей (цены районов Сбербанк) 🎉
✅ lots             — 0 (ждём оркестратор 00:00 UTC)
✅ auctions         — 0
✅ messages         — 0
✅ documents        — 0
✅ price_schedules  — 0
✅ system_state     — 1 запись

❌ НЕ СОЗДАНЫ: antifraud_results, managers, watchlist
```

### Датасеты:
```
data/ не в git (в .gitignore) — нужен rsync с локальной машины на VPS
data/cadastral/ — 113 MB, 584к кадастровых записей Москвы
data/sberbank/  — 45 MB (market_benchmarks.csv, train.csv)
import_datasets.sql — скрипт загрузки в БД
```

---

## 🔌 CHECKO API — документация

```
Base URL: https://api.checko.ru/v2/
Ключ: CHECKO_API_KEY в .env

ENDPOINTS:
/company?inn=...             → юрлицо (ЕГРЮЛ)
/entrepreneur?inn=...        → ИП (ЕГРИП)
/person?inn=...              → физлицо
/search?by=...&obj=...       → поиск (name/founder-name/leader-name/okved)
/finances?inn=...            → финотчётность (Росстат + ФНС)
/legal-cases?inn=...         → арбитражные дела (КАД, задержка 1-2 нед.)
/enforcements?inn=...        → исполнительные производства (ФССП)
/inspections?inn=...         → проверки (Генпрокуратура)
/bankruptcy-messages?inn=... → записи ЕФРСБ
/fedresurs?inn=...           → сообщения Федресурса
/contracts?inn=&law=44       → госконтракты (44-ФЗ/94-ФЗ/223-ФЗ)
/timeline?inn=...            → история изменений
/bank?bic=...                → банк по БИК

АНТИФРОД — флаги из /company:
  МассРуковод / МассУчред    → массовые директор/учредитель (ФНС)
  ДисквЛицо / ДисквЛица      → дисквалификация
  Санкции + СанкцУчр         → санкционные списки (правило 50%)
  НелегалФин                 → нелегальная деятельность (ЦБ)
  НедобПост                  → реестр недобросовестных поставщиков
  ЮрАдрес.Недост             → недостоверный адрес
  ЮрАдрес.МассАдрес[]        → массовый адрес
  ЕФРСБ[]                    → записи о банкротстве
  УстКап.Сумма               → уставный капитал (подозрительно малый)

СКОРИНГ — полезные поля:
  Налоги.СумУпл              → косвенный оборот
  СЧР                        → среднесписочная численность
  Руковод[].СвязРуковод[]    → ОГРН других компаний директора
  СвязУчред[]                → дочерние компании

meta.status / meta.balance / meta.today_request_count
```

---

## 📦 СПРИНТЫ

```
Sprint 1 — FastAPI + PostgreSQL + Docker
Sprint 2 — Telegram + Rosreestr + Semantic + Red flags          (влит)
Sprint 3 — Checko + Document extractor + Research API           (влит 18.02)
Sprint 4 — Переписан поиск, API клиенты, аудит                 (текущий)
```

---

## 🛡️ RESOURCE MONITOR

```
cpu_threshold: 80%  → пауза 10 сек
cpu_critical: 150%  → пауза 60 сек  ← был случай >200%!
ram_threshold: 85%
ram_critical:  95%
```

---

## 🚀 КОМАНДЫ

```bash
cd /root/fedr

# Docker
docker-compose up -d
docker-compose down && docker-compose build && docker-compose up -d
docker logs -f fedr-app-1

# Лимиты API
curl -s "https://parser-api.com/stat/?key=ede50185e3ccc8589a5c6c6efebc14cc"

# Поиск
docker exec -it fedr-app-1 python -m src.services.fedresurs_search

# БД
docker exec fedr-db-1 psql -U postgres -d fedresurs_db -c "\dt"
docker exec fedr-db-1 psql -U postgres -d fedresurs_db -c "SELECT relname, n_live_tup FROM pg_stat_user_tables ORDER BY n_live_tup DESC;"

# Миграции
alembic upgrade head
```

---

## 💡 ВАЖНЫЕ ЗАМЕТКИ

1. БД — `fedresurs_db`, user `postgres` (не fedruser, не fedr!)
2. get_trades / get_trade_messages — НЕ СУЩЕСТВУЮТ
3. CPU — был случай >200%, хостер предупреждал об отключении
4. Запросы — НИКОГДА параллельно, только через asyncio.Lock()
5. arbitr/reestr/mosgorsud — только для score >= 70
6. 76 лотов — целевая цифра (найдено вручную на сайте)
7. Claude Code — иногда описывает планы как сделанное. Проверять на сервере!

---

## 📅 ИСТОРИЯ СЕССИЙ

### Сессия 1 — 15.02.2026
**Сделано:**
- ✅ Xray VPN (split-routing)
- ✅ FastAPI + Docker
- ✅ Vue 3 + Nginx
- ✅ Mock данные на дашборде

---

### Сессия 2 — 16.02.2026
**Сделано:**
- ✅ Resource Monitor
- ✅ SmartParserClient с лимитами
- ✅ CLAUDE.md создан

---

### Сессия 3 — 17.02.2026
**Сделано:**
- ✅ Найдена ошибка: get_trades НЕ СУЩЕСТВУЕТ в Parser API
- ✅ Прочитана полная документация fedresurs_api и arbitr_api
- ✅ Переписан fedresurs_search.py (правильный pipeline)
- ✅ Исправлены лимиты: arbitr/reestr/mosgorsud = 200/месяц (обновляются)

---

### Сессия 4 — 18.02.2026
**Сделано:**
- ✅ Аудит БД: fedresurs_db, 7 таблиц, все пустые
- ✅ Созданы rosreestr_client.py, checko_client.py, moscow_api_client.py
- ✅ checko_client.py — все endpoints реализованы
- ✅ Sprint 3 влит в master, миграции применены (a881e873d6f2)
- ✅ Docker пересобран, зависимости обновлены
- ✅ Документация Checko API собрана
- ✅ orchestrator.py исправлен (get_trade_messages → FedresursSearch)
- ✅ Защита лимитов добавлена (_check_api_limits + _seconds_until_midnight)
- ✅ system_state сброшен с 2026-01-24 на NOW()
- ✅ Парсинг /stat/ исправлен (API возвращает массив, не словарь)
- ✅ Датасеты залиты на VPS (rsync + volume mount)
- ✅ Загружено в БД: 584,354 кадастровых записей + 60 бенчмарков
- ✅ Критический тест поиска пройден — защита лимитов работает
- ✅ STATUS.md создан для NotebookLM

**Статус на конец сессии (22:48 МСК):**
- Оркестратор спит до 00:00 UTC (лимит 250/250, осталось 0)
- Тест поиска: 0 запросов потрачено, ошибка 40304 обработана корректно
- Лимит обновится в 00:00 UTC = 03:00 МСК 19.02
- cadastral_index и market_benchmarks готовы для обогащения лотов
- Система готова к первому реальному сбору данных

---

### Сессия 5 — 18.02.2026 (ночь)

**Проблемы обнаружены:**
- ❌ Порог защиты лимитов 240/250 — слишком высокий, не оставлял запаса на отладку
- ❌ Бесконечная петля оркестратора — каждые 10 сек запускался заново
- ❌ `get_last_processed_date()` возвращал `None` → "первый запуск" бесконечно
- ❌ `return` внутри `try/finally` с `break` → Python перезаписывал return
- ❌ FedresursSearch падал на `None.lower()` в description

**Исправлено агентом:**
- ✅ Баг #1: `.lower()` на None — добавлена защита `(x or "").lower()`
- ✅ Баг #2: State не обновлялся при ошибке — добавлен `finally` блок
- ✅ Баг #3: Бесконечная петля — исправлена логика `get_last_processed_date()`
- ✅ Python quirk с `return/finally/break` — убран `break`, результат в переменную

**Статус на 18.02 08:12 МСК:**
- Оркестратор работает корректно
- Следующий поиск: через 358 минут (~14:00 МСК 18.02)
- Использовано запросов: 240/250
- Осталось запросов: 10

---

### Сессия 6 — 19.02.2026 (вечер)
**Исполнитель:** deepseek/deepseek-v3.2
**Задачи:** TASK-005 (Telegram уведомления), TASK-007 (починить ветки git), TASK-006 (навести порядок в репозитории)

**Сделано:**
- ✅ TASK-005: Проверка Telegram бота @dev2dev1_bot — токен и chat_id настроены в .env, тестовое сообщение отправлено вручную, бот работает.
- ✅ TASK-007: Исправление веток git:
  1. Убедился что все изменения из claude/fedresurs-mvp-panel-CEGUc закоммичены.
  2. Смержил в master: `git checkout master && git merge claude/fedresurs-mvp-panel-CEGUc && git push origin master`.
  3. Сделал master default веткой на GitHub (через Settings → Branches).
  4. Удалил лишнюю ветку: `git push origin --delete claude/fedresurs-mvp-panel-CEGUc && git branch -d claude/fedresurs-mvp-panel-CEGUc`.
- ✅ TASK-006: Наведение порядка в репозитории:
  1. Создана папка `scripts/debug/`.
  2. Перемещены все отладочные скрипты из корня в `scripts/debug/` (список из ~30 файлов).
  3. Корень репозитория очищен, оставлены только необходимые директории и файлы.
  4. Выполнен коммит и пуш в ветку `claude/fedresurs-mvp-panel-CEGUc`.
- ✅ Обновлён TASKS.md с отметками ✅ ВЫПОЛНЕНО 19.02.2026 для TASK-005, TASK-006, TASK-007.
- ✅ Сессионное резюме добавлено в Claude.md.

**Статус на конец сессии (21:53 UTC):**
- Telegram бот готов к уведомлениям о лотах.
- Ветка master теперь содержит весь актуальный код, default branch = master, старая ветка удалена.
- Репозиторий очищен от отладочных скриптов, структура стала чище.
- Система готова к дальнейшему обогащению лотов и скорингу.

---

## ❌ ТЕКУЩИЕ ПРОБЛЕМЫ

1. **fedresurs 250/250** — обновится в 00:00 UTC (03:00 МСК 19.02)

---

## 🎯 ЗАДАНИЕ (19.02 утром после 03:00 МСК)

Проверить что оркестратор нашёл лоты и обогатить их:

```bash
# 1. Логи поиска
docker logs fedr-app-1 2>&1 | grep "Найдено\|Сохранено\|лимит\|Found"

# 2. Количество лотов
docker exec fedr-db-1 psql -U postgres -d fedresurs_db \
  -c "SELECT COUNT(*) FROM lots;"

# 3. Первые лоты (детали)
docker exec fedr-db-1 psql -U postgres -d fedresurs_db \
  -c "SELECT id, description, start_price, debtor_inn FROM lots LIMIT 5;"
```

Если лоты есть → подключать pipeline обогащения:
1. rosreestr_client.py → кадастр, площадь, координаты
2. checko_client.py → директор, антифрод (используя debtor_inn)
3. moscow_api_client.py → охранные зоны, ПЗЗ
4. Скоринг → deal_score = investment - fraud*0.6
5. Telegram уведомления для score >= 80

---

## 🎯 ЗАДАНИЕ (19.02 после 14:00 МСК)

**Исполнитель: Claude Code**

Когда оркестратор проснётся (~14:00 МСК):
```bash
# 1. Проверить логи поиска
docker logs fedr-app-1 2>&1 | grep -E "Найдено|Found|ERROR" | tail -30

# 2. Проверить лоты в БД
docker exec fedr-db-1 psql -U postgres -d fedresurs_db \
  -c "SELECT COUNT(*) FROM lots;"

# 3. Если лотов 0 — смотреть детали ошибки
docker logs fedr-app-1 2>&1 | grep -B 5 -A 5 "ERROR"

# 4. Проверить остаток лимита
curl -s "https://parser-api.com/stat/?key=ede50185e3ccc8589a5c6c6efebc14cc"
```

**Если найдены лоты (N > 0):**
- Подключать pipeline обогащения (Checko + Rosreestr)
- Запускать скоринг
- Проверять Telegram уведомления

**Если лотов 0:**
- Анализировать логи ошибок
- Проверять фильтр (может слишком строгий)
- Ждать следующего цикла (ещё 6 часов)

---

### Сессия 7 — 21.02.2026 (вечер)
**Исполнитель:** Claude Code
**Задачи:** TASK-009 (Оптимизация API-запросов), TASK-010 (Интеграция /v1/trade-messages (ЭТП))

**Сделано:**

#### TASK-009: Оптимизация API-запросов
1. **Добавлен `includeBankruptInfo=True` в запрос `search_ur`** — теперь данные о должнике приходят сразу в ответе, не требуется отдельный запрос `get_bankrupt_info()`.
2. **Увеличены лимиты до 1000**:
   - `orgs_per_request`: 100 → 1000 (организаций за один запрос)
   - `msgs_per_request`: 100 → 1000 (сообщений за один запрос)
3. **Добавлен параметр `limit` в запросы**:
   - `_get_orgs_page` теперь использует `limit=SEARCH_CONFIG["orgs_per_request"]`
   - `_get_org_messages` теперь использует `limit=SEARCH_CONFIG["msgs_per_request"]`
4. **Убраны отдельные запросы `get_bankrupt_info()`** — данные уже включены в ответ `search_ur` с параметром `includeBankruptInfo`.

**Результат:** Сокращение количества запросов с ~37 до ~5-10 на цикл обработки 35 организаций.

#### TASK-010: Интеграция /v1/trade-messages (ЭТП)
1. **Добавлены новые методы в `FedresursSearch`**:
   - `get_trade_messages(published_after, region_id=77, limit=1000)` — получение сообщений о торгах через ЭТП
   - `get_trade_message_content(guid)` — получение содержимого сообщения по GUID
2. **Добавлены новые поля в модель `Lot`**:
   - `etp_url TEXT` — URL электронной торговой площадки
   - `etp_name TEXT` — название ЭТП
   - `application_start TIMESTAMPTZ` — дата начала приёма заявок
   - `application_end TIMESTAMPTZ` — дата окончания приёма заявок
   - `organizer_name TEXT` — имя организатора торгов
3. **Создана и применена миграция**:
   - `alembic revision --autogenerate -m "add etp fields to lots"`
   - `alembic upgrade head` (миграция `7dfc5094ad39`)
4. **Интегрировано в пайплайн поиска**:
   - Метод `_filter_lot` теперь извлекает ЭТП данные из сообщения (если присутствуют) и добавляет их в результат
   - Поддержка полей: `etp_url`, `etp_name`, `application_start`, `application_end`, `organizer_name`

**Результат:** Система теперь может получать и сохранять данные об электронных торговых площадках, что улучшает качество информации о лотах.

**Обновления файлов:**
- `src/services/fedresurs_search.py` — добавлены новые методы и оптимизации
- `src/database/models.py` — добавлены новые поля в модель Lot
- `TASKS.md` — обновлены статусы заданий 9 и 10 как выполненные

**Статус на конец сессии (21.02.2026):**
- Оптимизация API-запросов завершена, ожидается сокращение расхода лимитов
- Интеграция ЭТП данных выполнена, система готова к сбору дополнительной информации о торгах
- Все изменения закоммичены и готовы к тестированию в следующем цикле оркестратора
