
# Техническое задание для Claude Code
## Спринт 3: Интеграция Checko.ru API + Research Service

---

## 🎯 Цель спринта

Внедрить систему комплексного анализа объектов недвижимости (Research Service), которая автоматически прогоняет объект через 4 источника данных и генерирует досье с оценкой рисков.

---

## 📋 Задачи

### Задача 1: Создать клиент для Checko.ru API

**Файл:** `src/services/checko_client.py`

**Требования:**

1. Класс `CheckoAPIClient` с async/await паттерном
2. Методы для работы с API:
   - `get_company_brief(inn)` — краткая информация о компании
   - `get_finance(inn)` — финансовая отчётность
   - `get_relations(inn)` — связанные компании и учредители
   - `get_arbitrations(inn)` — арбитражные дела
   - `get_enforcement(inn)` — исполнительные производства
   - `search_person(fio)` — поиск физлица
   
3. Специальные методы:
   - `check_bankruptcy_risk(inn)` — комплексная оценка риска банкротства
     - Возвращает: `{"risk_level": "LOW/MEDIUM/HIGH/CRITICAL", "score": 0-100, "factors": [], "recommendations": []}`
     - Логика скоринга:
       - Статус "liquidating"/"bankrupt" → +100 баллов (CRITICAL)
       - Отрицательный капитал → +40 баллов
       - Убытки за год → +20 баллов
       - Активные исполнительные производства → +10 за каждое (макс 30)
       - Арбитражные дела (как ответчик) → +15 если >5 дел
   
   - `find_hidden_assets(inn)` — поиск компаний через учредителей
     - Через `get_relations()` получаем список учредителей-физлиц
     - Для каждого физлица вызываем `search_person()` → получаем список других компаний
     - Возвращаем массив: `[{company_inn, company_name, founder, role, share}]`

4. Технические требования:
   - Использовать `httpx.AsyncClient`
   - Базовый URL: `https://api.checko.ru/v2`
   - Авторизация: `Authorization: Token {api_key}`
   - Таймаут: 30 секунд
   - Обработка ошибок: логирование через `loguru`, возврат `{"error": "..."}` при провале

5. Context manager паттерн:
   ```python
   async with CheckoAPIClient(api_key) as client:
       data = await client.get_company_brief("7713321151")
   ```

**Документация API:** https://checko.ru/integration/api

---

### Задача 2: Создать Research Service

**Файл:** `src/services/research.py`

**Требования:**

1. Класс `ResearchService` со следующими зависимостями:
   - `CheckoAPIClient` (Checko)
   - `ParserAPIClient` (уже существует в `src/services/external_api.py`)
   - `RosreestrEnricher` (уже существует в `src/services/enricher.py`)
   - `AsyncSession` (для работы с БД)

2. Основной метод: `research_property(cadastral_number: str, owner_inn: Optional[str]) -> Dict`

**Структура возвращаемого отчёта:**

```python
{
    "cadastral_number": "77:01:0004022:1026",
    "research_date": "2026-02-05T18:30:00",
    
    "property": {
        "area": 512.2,                    # из Росреестра
        "cadastral_value": 135500000,     # из Росреестра
        "permitted_use": "...",           # ВРИ из Росреестра
        "address": "...",                 # из Росреестра
        "pkk_url": "https://pkk.rosreestr.ru/#/search/...",
        "isogd_url": "https://isogd.mos.ru/isogd-portal/landing?cadnum=..."
    },
    
    "owner": {
        "inn": "7713084767",
        "name": "ООО «ОМДА»",
        "status": "active",              # из Checko
        "revenue": 13800000,             # из Checko (finance)
        "profit": 5000000,               # из Checko (finance)
        "net_assets": 21200000,          # из Checko (finance)
        "bankruptcy_risk": {             # из check_bankruptcy_risk()
            "risk_level": "MEDIUM",
            "score": 35,
            "factors": [...],
            "recommendations": [...]
        },
        "related_companies": 6,          # кол-во из get_relations()
        "founders": [...]                # из get_relations()
    },
    
    "legal_risks": {
        "arbitration_cases": 2,          # из Parser API
        "bankruptcy_cases": 0,           # из Parser API
        "pledges": [],                   # из Parser API (по кадастру)
        "risk_level": "LOW/MEDIUM/HIGH/CRITICAL"
    },
    
    "market_analysis": {
        "cadastral_quarter": "77:01:0004",
        "price_per_sqm": 264589,
        # TODO: В будущем добавить медианную цену по кварталу из БД
    },
    
    "final_score": 55,                   # 0-100
    "recommendations": [
        "⚠️ Умеренный риск. Требуется дополнительная проверка",
        "⚠️ У владельца 6 связанных компаний → проверить схемы дробления"
    ]
}
```

3. Логика скоринга `_calculate_final_score(report)`:
   - Базовый балл: 50
   - **Положительные факторы:**
     - Площадь известна → +5
     - ВРИ указан → +5
   - **Отрицательные факторы:**
     - Bankruptcy risk CRITICAL → -50
     - Bankruptcy risk HIGH → -30
     - Bankruptcy risk MEDIUM → -15
     - Legal risk CRITICAL → -40
     - Legal risk HIGH → -25
     - Legal risk MEDIUM → -10
     - Есть залоги → -20
   - Итоговый балл: `max(0, min(100, score))`

4. Метод `_generate_recommendations(report) -> List[str]`:
   - Если `final_score >= 70`: "✅ Объект рекомендуется к рассмотрению"
   - Если `final_score >= 40`: "⚠️ Умеренный риск. Требуется дополнительная проверка"
   - Если `final_score < 40`: "🚫 Высокий риск. Не рекомендуется без глубокого Due Diligence"
   - Дополнительные рекомендации на основе флагов:
     - Bankruptcy risk HIGH/CRITICAL → "⚠️ Высокий риск банкротства продавца → требуется аккредитив"
     - Есть залоги → "⚠️ Имущество в залоге → требуется согласие залогодержателя"
     - Нет площади → "⚠️ Площадь неизвестна → требуется выписка из ЕГРН"
     - Отрицательный капитал → "🚨 Отрицательный капитал → возможен вывод активов"
     - >5 связанных компаний → "⚠️ У владельца {N} связанных компаний → проверить схемы дробления"

5. Утилита для форматирования: `format_research_report_markdown(report) -> str`
   - Генерирует Markdown-отчёт для отправки в Telegram
   - Структура: заголовок, объект, владелец, юр.риски, итоговая оценка, рекомендации

---

### Задача 3: Создать FastAPI endpoints

**Файл:** `src/api/research_routes.py`

**Роутер:** `APIRouter(prefix="/api/research", tags=["Research"])`

**Endpoints:**

1. `GET /api/research/{cadastral_number}`
   - Query параметры:
     - `owner_inn` (Optional[str]) — ИНН владельца
     - `format` (str, default="json") — "json" или "markdown"
   - Вызывает `ResearchService.research_property()`
   - Если `format="markdown"`, возвращает `{"format": "markdown", "content": "..."}`

2. `POST /api/research/batch`
   - Body: `[{"cadastral_number": "...", "owner_inn": "..."}, ...]`
   - Лимит: максимум 10 объектов за раз
   - Возвращает: `{"total": N, "results": [...]}`

3. `GET /api/research/company/{inn}`
   - Возвращает полное досье на компанию:
     - `company` (из `get_company_full()`)
     - `bankruptcy_risk` (из `check_bankruptcy_risk()`)
     - `hidden_assets` (из `find_hidden_assets()`)

4. `GET /api/research/examples/omda`
   - Демонстрационный эндпоинт
   - Hardcoded: `cadastral_number="77:01:0004022:1026"`, `owner_inn="7713084767"`
   - Для тестирования всей цепочки

5. `GET /api/research/examples/oteko`
   - Демонстрационный эндпоинт
   - Hardcoded: `owner_inn="7713321151"`, кадастр нужно уточнить

**Интеграция в main.py:**
```python
from src.api.research_routes import router as research_router
app.include_router(research_router)
```

---

### Задача 4: Обновить конфигурацию

**Файл:** `src/core/config.py`

**Добавить:**

```python
# ========== External APIs ==========

# Parser API (уже есть)
PARSER_API_KEY: str = "ede50185e3ccc8589a5c6c6efebc14cc"
PARSER_API_BASE_URL: str = "https://parser-api.com/parser"

# Checko.ru API (НОВОЕ)
CHECKO_API_KEY: Optional[str] = None  # Нужно получить после регистрации
```

**Файл:** `.env.example`

Добавить строку:
```
CHECKO_API_KEY=your_checko_api_key_here
```

---

### Задача 5: Создать документацию

**Файл:** `docs/RESEARCH_API.md`

**Содержание:**

1. Описание Research Service
2. Примеры запросов с `curl`
3. Примеры ответов (JSON)
4. Инструкция по получению Checko API ключа
5. Описание метрик скоринга
6. Рекомендации по использованию (когда вызывать, как экономить квоты)

---

## 📊 Структура файлов (итог)

```
src/
├── services/
│   ├── checko_client.py          # НОВЫЙ
│   ├── research.py                # НОВЫЙ
│   ├── enricher.py                # СУЩЕСТВУЕТ
│   └── external_api.py            # СУЩЕСТВУЕТ (Parser API)
├── api/
│   ├── research_routes.py         # НОВЫЙ
│   └── routes.py                  # СУЩЕСТВУЕТ
└── core/
    └── config.py                  # ОБНОВИТЬ

docs/
└── RESEARCH_API.md                # НОВЫЙ
```

---

## 🔗 Внешние зависимости

### Checko.ru API

- **Документация:** https://checko.ru/integration/api
- **Регистрация:** https://checko.ru/integration/api (нужен email)
- **Тарифы:**
  - Базовый: 100 запросов/день
  - Бизнес: 1000 запросов/день
- **Базовый URL:** `https://api.checko.ru/v2`
- **Авторизация:** Header `Authorization: Token {api_key}`

### Методы API (которые используем):

```
GET /company?key={key}&inn={inn}               # Краткая информация
GET /company?key={key}&inn={inn}&type=finance  # Финансы
GET /company?key={key}&inn={inn}&type=relations # Связи
GET /company?key={key}&inn={inn}&type=arbitrations # Арбитраж
GET /company?key={key}&inn={inn}&type=enforcement # Исполнительные производства
GET /person?key={key}&fio={name}               # Физлицо
```

---

## 🧪 Тестирование

### Эталонные объекты для проверки:

1. **ООО "ОМДА"** (Воротниковский пер., 10с4)
   - Кадастр: `77:01:0004022:1026`
   - ИНН: `7713084767`
   - Ожидаемые риски:
     - ✅ Земля в аренде
     - ✅ Схема дробления (связка с ООО "СИС")
     - ✅ Отрицательный капитал в связанной компании "АИР МАКС"

2. **АО "ОТЭКО"** (Лужнецкая наб., 10Б)
   - ИНН: `7713321151`
   - Ожидаемые риски:
     - ✅ Бенефициар под санкциями
     - ✅ Процесс реорганизации
     - ✅ СЗЗ на участке

### Команды для теста:

```bash
# Запуск сервера
uvicorn src.api.main:app --reload

# Тест 1: ОМДА
curl http://localhost:8000/api/research/examples/omda

# Тест 2: Проверка компании
curl http://localhost:8000/api/research/company/7713321151

# Тест 3: Конкретный объект
curl "http://localhost:8000/api/research/77:01:0004022:1026?owner_inn=7713084767"
```

**Ожидаемый результат:** JSON с заполненными полями `property`, `owner`, `legal_risks`, `final_score`, `recommendations`.

---

## ⚠️ Важные замечания

1. **Квоты API:**
   - Checko: 100 запросов/день (базовый тариф)
   - Parser API: 200 запросов/месяц
   - **Стратегия:** Вызывать Research Service только для высокоприоритетных лотов (GARDEN_RING, TTK, TPU)

2. **Кэширование (TODO для будущего):**
   - Создать таблицу `checko_cache` в БД
   - TTL: 30 дней для финансов, 7 дней для базовой инфо
   - Проверять кэш перед каждым запросом к API

3. **Обработка ошибок:**
   - Если Checko API недоступен → продолжить работу без секции `owner`
   - Если Росреестр не вернул данные → заполнить `property.error`
   - Никогда не ломать весь pipeline из-за провала одного источника

4. **Безопасность:**
   - API-ключи только в `.env` (никогда не коммитить в Git)
   - Добавить `.env` в `.gitignore`

---

## 📝 Критерии готовности (Definition of Done)

- [ ] `CheckoAPIClient` создан со всеми методами
- [ ] `ResearchService` работает и возвращает корректный JSON
- [ ] FastAPI endpoints отвечают на запросы
- [ ] Эндпоинт `/api/research/examples/omda` возвращает данные с expected risks
- [ ] Документация `RESEARCH_API.md` создана
- [ ] `.env.example` обновлён
- [ ] `config.py` содержит `CHECKO_API_KEY`
- [ ] Все методы логируют ошибки через `loguru`
- [ ] Код прошёл тестирование на эталонных объектах

Результаты собеди в файл tests.md

---

## 🚀 Следующие спринты (Roadmap)

После завершения этого спринта:

- **Спринт 4:** Антифрод v2.0 (сравнение с медианой по кварталу вместо кадастровой стоимости)
- **Спринт 5:** Интеграция в Telegram (автоотправка досье при обнаружении интересного лота)
- **Спринт 6:** Таблица кэша + фоновый воркер обновления данных
- **Спринт 7:** Neo4j для построения графа связей (полноценный Manager Karma)



---

**Дата создания ТЗ:** 2026-02-05  
**Версия:** 1.0  
**Автор:** Senior Architect

Общаемся на Русском