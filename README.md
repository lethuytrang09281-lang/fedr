# Fedresurs Radar

Проект для парсинга и анализа данных с Единого федерального реестра сведений о банкротстве (ЕФРСБ).

## Запуск парсера локально (без Docker)

Если Docker капризничает, запускаем проект напрямую.

### Предварительные требования

- Python 3.11+ установлен.
- PostgreSQL установлен и запущен.
  - Скачать для Windows/Mac: postgresql.org/download
  - При установке запомни пароль пользователя postgres.
  - Port: 5432

### Пошаговая установка

#### 1. Создание базы данных

Открой терминал или pgAdmin и создай пустую базу:

```sql
CREATE DATABASE fedresurs_db;
```

#### 2. Виртуальное окружение

В папке проекта выполни команды:

Windows:

```bash
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

#### 3. Проверка настроек

Убедись, что файл `.env` (который мы создали выше) лежит рядом с `requirements.txt` и пароль `DB_PASSWORD` подходит к твоему локальному Postgres.

#### 4. Инициализация таблиц

Запусти скрипт создания таблиц. Если он пройдет без ошибок, значит база подключена верно.

```bash
python init_db.py
```

Должно вывести: `База данных инициализирована успешно!`

#### 5. Запуск Оркестратора 🚀

Запускаем основной скрипт.

```bash
python src/main.py
```

### Настройка окружения (.env)

Создай файл `.env` в корне проекта. Это файл конфигурации, который `src/config.py` будет читать автоматически. Убедись, что данные от базы совпадают с твоим локальным Postgres.

```env
# --- Настройки API Федресурса ---
EFRSB_ENV=DEMO
# Используем демо-логин (он публичный)
EFRSB_LOGIN=demowebuser
EFRSB_PASSWORD=Ax!761BN

# Лимиты (локально можно поставить меньше, чтобы не нагружать сеть)
MAX_REQS_PER_SECOND=5

# --- Настройки Базы Данных (Localhost) ---
# Убедись, что у тебя установлен PostgreSQL и создан пользователь/база
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin        # Поменяй на свой пароль от локального Postgres
DB_NAME=fedresurs_db     # Эту базу надо создать руками через pgAdmin или psql
```

### Что делать, если есть ошибки?

- **Ошибка Connection refused (Postgres):**
  - Проверь, запущен ли Postgres (в Службах Windows или через systemctl).
  - Проверь, правильный ли пароль в `.env`.

- **Ошибка Module not found:**
  - Ты забыл активировать venv или сделать pip install.
  - Проверь, что запускаешь скрипт из корня проекта: `python src/main.py`, а не `python main.py` из папки src.

- **Ошибка 401 (API):**
  - Возможно, демо-пароль Федресурса сменился или сервер недоступен. Попробуй зайти на bank-publications-demo.fedresurs.ru через браузер.